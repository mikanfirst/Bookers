<h1>Books</h1>
<% @books.each do|book| %>
  <p>title</p>
  <span><%= book.title %></span>
  <p>Body</p>
  <span><%= book.body %></span>
  <%= link_to "destroy", book_path(book.id), method: :delete %>
<% end %>